# que = Write a program to Recursive Fibonacci. 
def fib(n): #defining function
    if (n == 0 or n == 1): # base condition
        return n
    return fib(n-2) + fib(n-1)

num = int(input("enter the nunber = "))
for i in range(num):
    print(fib(i), end=" ")


